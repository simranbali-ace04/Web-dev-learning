console.log("Hello World!");
/*behind the scenes:
1.) before the code run-time
source code - - - -> syntax tree
           via parse (taking keywords and assembling it)
2.) JIT compiler (just in time compiler)
              next
[Byte code(before 0 and 1 code conversion as this byte code fastens the binary code conversion)----> machine code ]
              next
 code execution
*/
